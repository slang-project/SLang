unit Integer is
end

i: is Integer(0)// - проходит? Проходит, и результат ноды в дереве 
                // показан на картинке isInteger_WITH_colon - там вызов Call стоит на одном уровне с объявлением переменной
                // если не ставить двоеточие - 
