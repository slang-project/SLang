/*unit Integer is end

abstract unit A is
    unit B is foreign
	  foo(): Integer is foreign
	end
	
	foo(): Integer is foreign
end

abstract unit A1 is
    abstract unit B1 is end
end	

unit A2 is
    unit B2 is end
end	
*/
// Два раза v в дереве?
v: UnknownType
/*
unit X extend UnknownType is end
*/
